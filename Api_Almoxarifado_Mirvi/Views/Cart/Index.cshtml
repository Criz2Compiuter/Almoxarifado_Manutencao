@using Api_Almoxarifado_Mirvi.Models.ViewModels;
@model CarrinhoCompraViewModel

<div class="row checkoutForm">

    <div>
        <h4> Seu carrinho de compras : </h4>
    </div>
    <table class="table table-bordered table-striped">
        <thead>
            <tr>
                <th>Quantidade</th>
                <th>Produto</th>
                <th class="text-right">Preço</th>
                <th class="text-right">Subtotal</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.CartBuy.CarrinhoCompraItens)
            {
                <tr>
                    <td class="text-center">@item.Quantidade</td>
                    <td class="text-left">@item.Produto.Descricao</td>
                    <td class="text-right">@item.Produto.Valor.ToString("c")</td>
                    <td class="text-right">
                        @((item.Quantidade * item.Produto.Valor).ToString("c"))
                    </td>
                    <td class="text-center">
                        <a asp-controller="CarrinhoCompra" asp-action="RemoverItemDoCarrinhoCompra" asp-route-lancheId="@item.Produto.Id"></a>
                    </td>
                </tr>
            }
        </tbody>
        <tfoot>
            <tr>
                <td colspan="3" class="text-right">Total:</td>
                <td class="text-right">
                    @Model.carrinhoCompraTotal.ToString("c")
                </td>
            </tr>
        </tfoot>
    </table>

    @*<div class="text-right">
            <div class="btn-group ">
                <a class="btn btn-success" asp-controller="Pedido" asp-action="Checkout">Encerrar Pedido</a>
                <a class="btn-group">
                    <a class="btn btn-default dropdown-toggle" data-toggle="dropdown">
                        Adicionar mais lanches<span class="caret"></span>
                    </a>
                    <ul class="dropdown-menu" role="menuitem">
                        <li><a asp-controller="Lanche" asp-action="List" asp-route-categoria="Normal"><b>Normal</b></a></li>
                        <li><a asp-controller="Lanche" asp-action="List" asp-route-categoria="Natural"><b>Natural</b></a></li>
                    </ul>
                </a>
            </div>
        </div>*@
    <div class="text-right">
        <div class="btn-group ">
            <a class="btn btn-success" asp-controller="Pedido" asp-action="Checkout">Encerrar Pedido</a>
            <a class="btn-group">
                <a class="btn btn-default dropdown-toggle" data-toggle="dropdown">
                    Mais Lanches  <span class="caret"></span>
                </a>
            </a>
        </div>
    </div>

</div>

