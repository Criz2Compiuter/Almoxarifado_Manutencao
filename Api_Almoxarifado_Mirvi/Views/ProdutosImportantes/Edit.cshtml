@model Api_Almoxarifado_Mirvi.Models.ViewModels.FormularioCadastroProdutoImportante
@{
    ViewData["Title"] = "Editar";
}

<h1>@ViewData["Title"]</h1>

<h4>Prateleira</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <input type="hidden" asp-for="ProdutoImportante.Id" />
            <div class="form-group">
                <label asp-for="ProdutoImportante.Id" class="control-label"></label>
                <input asp-for="ProdutoImportante.Id" class="form-control" />
            </div>
            <div class="form-group">
                <label asp-for="ProdutoImportante.Descricao" class="control-label"></label>
                <input asp-for="ProdutoImportante.Descricao" class="form-control" />
            </div>
            <div class="form-group">
                <p>Prateleira do Produto</p>
                <select asp-for="ProdutoImportante.PrateleirasId" asp-items="@(new SelectList(Model.Prateleira, "Id", "Nome"))" class="form-control"></select>
                <p>Endereco do Produto</p>
                <select asp-for="ProdutoImportante.EnderecosId" asp-items="@(new SelectList(Model.Endereco, "Id", "Nome"))" class="form-control"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>